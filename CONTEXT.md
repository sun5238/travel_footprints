# Travel Footprints（旅行足迹）

本地优先的个人旅行记忆软件：记录去过的城市、景点、店铺、美食、交通与爬山轨迹，并在本地地图上点亮回顾。单上下文（单机本地应用），不依赖任何账号或外部服务。

## 语言

**核心实体**

**Trip（行程）**:
一次独立的旅行，通常由一条便签文本导入而来；聚合城市、地点标记、交通段与轨迹。
_Avoid_: Journey, Vacation, 游记

**City（城市）**:
全局地图点亮的最小单元；有到访记录的城市才被点亮。
_Avoid_: Destination（机票语境有歧义）

**Pass-through（路过）**:
只在交通段中出现、没有 Visit 的地点；不点亮，仅在行程详情中可见。
_Avoid_: Transit, 途经

**Place（地点标记）**:
可被标记的具体地点，分三类：景点（scene）、店铺（shop）、地标（landmark）。名称以用户日志写法为权威。
_Avoid_: Marker（marker 是地图 UI 概念）, Spot, 打卡点

**Visit（到访）**:
一次在某地点标记的具体到访：时间、星级、评价、当时地点名称快照、标签与媒体组。同一地点多次到访 = 多条 Visit。
_Avoid_: Check-in（社交签到语义）, 打卡

**TransportLeg（交通段）**:
两个地点之间的移动记录：出发/到达、方式（高铁/飞机/自驾/徒步等）、文字描述。
_Avoid_: Route, Segment

**Trail（轨迹）**:
一次户外路线记录（如爬山），带开始/结束时间，可选择携带 GPX 轨迹线，或仅有时间元数据+手绘近似路径。
_Avoid_: Track（GPX 文件层术语）, 路线

**Media（媒体）**:
照片或视频，作为 Visit 的 N:1 挂靠物；存原文件 + 缩略图/封面帧。
_Avoid_: Asset, Photo（特指照片子类）, 附件

**Account（用户账号）**:
本软件明确不存在的概念；无登录、无鉴权体系，所有数据归属本机用户。
_Avoid_: User（作为登录身份）, 账号

**导入与解析**

**Import（导入）**:
两段式流程：先文本生成行程骨架，再媒体分组挂靠。
_Avoid_: Sync（同步是另一回事）

**TripSkeleton（行程骨架）**:
文本解析输出的待确认行程结构（名称、日期、城市、地点候选、交通段、轨迹时间），用户确认后才入库。
_Avoid_: Draft（过于宽泛）

**Cluster（分组）**:
媒体按时间与 GPS 聚类出的照片/视频组；导入挂靠的操作单位是组，不是单张媒体。
_Avoid_: Batch（指上传来源批次）, Album（相册语义）

**Pending（待精分）**:
暂时无法挂到具体地点标记的媒体的状态；先挂行程/城市级，之后可再归位。
_Avoid_: Unassigned, 未分类

**Corpus（标注库）**:
每次人工校正产生的“原文 -> 标准 JSON”记录；是解析器学习的来源，也是未来本地模型的训练料。
_Avoid_: Training data, Log

**LearningDictionary（学习词典）**:
解析器从校正中逐步积累的词表（店名、动词、日期写法等），用于提升下次识别率。
_Avoid_: Rules（规则是静态部分）

**地图与存储**

**DataRoot（数据根目录）**:
自包含的数据目录（travel.db + media/ + thumbs/ 等），只含相对路径，拷贝目录即迁移。
_Avoid_: Database（单指 db 文件）

**Light up（点亮）**:
有到访记录的城市或地点标记在全局地图上的辉光状态。
_Avoid_: Highlight（泛化的高亮）

**OfflineMap（离线地图）**:
默认瓦片来源：本地 .mbtiles 矢量包，支持带版本号的区域包更新；在线图源是显式开启的例外。
_Avoid_: Tiles（指底层瓦片文件）
