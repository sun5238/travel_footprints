# 旅行足迹 Travel Footprints —— 产品设计文档

> 状态：经用户多轮设计评审确认，作为实现阶段的权威依据。
> 关联调研：[tech-comparison-report.md](tech-comparison-report.md)（技术路线）、[research-report.md](research-report.md)（开源生态）。

## 1. 定位与原则

- 本地优先的个人旅行记忆软件：记录去过的城市、景点、店铺、美食、交通和爬山轨迹，用地图点亮 + 时间轴 + 照片/视频回顾。
- **无账号、无登录、无外发数据**。任何网络动作（在线地图、地理编码、LLM 解析）都必须显式开启且默认关闭。
- 消费端是“本机服务 + 浏览器”：PC 是运行场所，手机在同一局域网内可查看和上传。
- 数据自包含、可迁移、不锁定在任何平台/应用中。

## 2. 系统形态

- 后端在本机启动本地服务（FastAPI），前端浏览器访问。
- 手机浏览器可访问同一局域网内的服务：查看看板、上传文本和媒体。
- 途中采集：手机本地便签 + 相机，零网络依赖；回家后连局域网批量导入。手机厂商云备份作为旅途安全网，本软件不承担途中备份。
- 多设备远程同步（Tailscale 等）列为后置可选，不做进 MVP。

## 3. 数据根目录与存储

数据根目录布局（相对路径，禁止任何绝对路径引用）：

```text
travel_data/
  travel.db        # SQLite 单文件，唯一事实来源
  media/           # 原图/原视频，按 visit/批次组织
  thumbs/          # 缩略图/封面帧
  exports/         # 导出的 zip（可选缓存）
```

- 目录内只存相对路径；文件名不依赖来源路径；schema 带版本号并提供迁移。
- 换机/迁移 = 整体拷贝数据根目录。
- 备份：导出 zip（travel.db + media + 清单）；缩略图（thumbs/）**不导出**，恢复后按需重新生成；导入**目前仅支持整体恢复**（覆盖目标数据目录，换机场景）；增量合并导入列为开放项。
- 删除：直接删除 + 单次确认；行程/地点/到访/单张媒体均可删除；误删通过备份 zip 整体恢复兜底。

## 4. 数据模型（逻辑模型）

- `trip`：行程。一条便签 = 一段行程（如“国庆 7 天”）。字段：名称、起止日期、文字、标签。
- `city`：城市。全局地图点亮的最小单元。字段：名称（标准化）、中文名、中心坐标、备注。
- `poi`：地点标记。类型 `scene`（景点）/ `shop`（店铺）/ `landmark`（地标）。名称以日志写法为权威；坐标可选（查不到/不确定 = 手动拖点或留空“无坐标”态）；同名分店并存、不强制合并。
- `visit`：到访。POI 下多次到访 = 多条 visit 时间线，各自有时间、星级（1-5）、评价文字、**当时地点名称快照**（店改名后，时间轴仍显示“去的时候的名字”）、标签、媒体组。
- `transport_leg`：交通段。from/to、方式（高铁/飞机/自驾/徒步/其他）、出发/到达时间、文字备注、顺序。
- `trail`：轨迹。开始/结束时间、可选登顶时间、GPX 文件（可选）、手绘轨迹（可选）、里程/爬升（可选，来源 GPX）、备注。
- `media`：媒体。kind = photo / video；保存原文件 + 缩略图（视频为封面帧）；拍摄时间、GPS 坐标与精度（可选）、来源批次。
- `pending_bucket`：待精分。无法挂到具体 POI 的媒体先挂行程/城市级，状态位区分“已归类/待精分”。
- `correction`：解析标注库。原文 -> 人工确认后的标准 JSON，供将来本地模型使用。
- `dictionary`：学习词典。用户校正时新学到的词（店名/动词/日期写法等）。

层级关系：`city 1:N poi`，`poi 1:N visit`，`visit 1:N media`；`trip` 聚合城市/POI/交通段/轨迹。
点亮规则：全局地图 = 有 visit 记录的城市点亮；**只有 visit 才点亮**——仅出现在交通段中的城市（路过）不点亮，只在行程详情中可见；进入城市 = 该城市内 POI 点亮。

时间规则：所有实体时间以「本地挂钟时间 + IANA 时区」存储，同时派生 Unix 时间戳用于排序与聚类；日志解析默认使用当前时区，媒体保留 EXIF 时间戳与 offset 原样提取。

## 5. 导入流程（两段式）

### 5.1 文本 -> 行程骨架

- 粘贴整段便签 -> 解析生成行程骨架（trip 名、起止日期、途经城市、POI 候选、交通段、轨迹时间）-> 校正页展示。
- 解析管线（规则驱动，**默认不用 LLM**）：
  1. 预清洗文本；
  2. 规则层：日期/时间表达、交通词表、常用动词词表、城市名表（预载行政区划公开数据 + 词典学习补充）；
  3. 生成骨架候选；
  4. 校正页：用户确认/补漏，每次校正写入 `correction` 标注库，新词写入 `dictionary`。
- 原则：**解析结果先展示后入库，绝不静默错认**；未识别段落保留原文、可全文搜索兜底。
- 将来可选开关：本地模型（Ollama + Qwen）复用 `correction` 标注数据做 few-shot/微调；默认关闭。

### 5.2 媒体 -> 分组挂靠

- 手机浏览器多选上传或电脑拖拽上传。
- 聚类：EXIF 时间 + GPS，默认阈值 30 分钟 / 500 米（可调）-> 分组。
- 去重：照片导入时全文件 SHA-256 哈希去重；视频用「文件名 + 大小 + 头尾分块哈希」折衷；已存在的文件跳过复制、直接复用。
- 缩略图：**首次访问时懒生成**并缓存到 thumbs/（视频为封面帧），导入过程不做批量缩略图；缩略图不随备份导出。
- 景点模式：组 -> 确认挂到骨架中已有景点。
- 店铺模式：组 -> 新建店铺 POI（名称来自日志/用户输入，坐标可选，手动拖点或留空）。
- 无法归属的组进“待精分”桶。
- 视频同流程：原样存储 + 自动抽取封面帧（ffmpeg），浏览器播放 MP4。
- 实况照片：iPhone 实况照片 = 同名 HEIC 静帧 + MOV 短视频；Android 动态照片 = 单个文件（JPEG 内嵌视频流）。M1 按“照片 + 视频”两条媒体原样导入（各自哈希去重；HEIC 缩略图暂不可生成但不报错，Pillow 缩略图失败即跳过）。M2 按“同名成对”合并为一条实况媒体（封面=静帧，可播放短视频）；解析内嵌动态照片需新增解析依赖，列为待评估项，未与用户确认前不引入。

## 6. 看板与页面

- 首页 = 汇总看板，地图优先 + 时间轴侧栏：
  - 点亮地图（MapLibre GL JS）：城市/POI 两级辉光；
  - 统计卡：城市数、景点数、美食数、轨迹数、照片/视频数、总里程；
  - 时间轴：行程 -> 日期 -> visit 事件，可展开；
  - 过滤栏：年份/时间范围、城市、POI 类型、是否带媒体、关键词全文、标签，可组合。
- 详情页：城市 -> POI -> visit 时间线 -> 媒体网格。
- 设置页：数据目录、导出/导入 zip、离线瓦片管理、在线图源开关（默认关）。

## 7. 地图与瓦片

- MapLibre GL JS + 本地 `.mbtiles` 矢量瓦片包（OSM/开放数据），全离线可用。
- 区域包带名称/版本/生成日期；看板提示底图陈旧；支持一键重新下载（解决底图更新）。
- 在线图源为显式开关、默认关。
- 已知取舍：OSM 国内街道级数据弱；高德 JS API 不支持离线；国内图源授权问题后议。点亮/照片/评价等语义数据不依赖底图新旧。

## 8. 轨迹

- 现场记录用现成工具（OpenTracks / 两步路，纯离线 GPS）-> 导出 GPX -> 本软件导入。
- GPX 导入：提取起止时间、里程、爬升、轨迹线，挂到行程，可选关联照片/媒体。
- 无 GPX：从日志解析开始/登顶/结束时间 + 文字描述；可在图上手绘近似路径（GeoJSON）。
- 统计按 GPX 原始精度，不做平滑。

## 9. 隐私承诺

| 动作 | 对外暴露 | 默认 |
|---|---|---|
| 只看离线瓦片 | 无 | 开 |
| 在线底图瓦片 | IP、浏览视口/中心坐标、缩放级别、请求时间；带 key 时关联应用画像 | 关 |
| 地理编码 / POI 搜索 | 具体坐标与查询词（“你在查哪家店”） | 关（不做该功能） |

- 自己的数据（照片/评价/店名文字）不经任何网络动作外发。
- 全软件无账号、无登录；局域网访问不做鉴权体系，如需可加一次性访问口令（后置）。

## 10. 技术栈与实施阶段

- 后端：Python FastAPI + SQLite（SQLAlchemy）。
- 前端：Vue 3 + MapLibre GL JS。
- 媒体处理：Pillow 缩略图、ffmpeg 视频封面帧。
- M1 最小可用（约 2-3 周业余）：数据模型 + 点亮地图 + 城市/POI/visit CRUD + 媒体上传查看 + 备份导出。
- M2 导入管道（约 2-3 周）：文本解析 + 骨架校正 + 媒体分组挂靠 + 待精分桶 + 看板/时间轴/过滤。
- M3 轨迹与地图完善（约 1-2 周）：GPX 导入 + 无 GPX 轨迹 + 离线瓦片包管理更新。

## 11. 留白默认值 / 开放项

- 分组阈值：时间 30 分钟 + 距离 500 米，可调。
- 标签：自由输入，不预置集合。
- 合并同名 POI：手动触发，可用“这两个是同一家”语义。
- 全文搜索：M1 用 SQL LIKE，M2 升级 SQLite FTS5 + jieba 中文分词。
- 待评估：在线图源与店名候选（高德 API key/配额/条款）、Tailscale 远程同步、本地模型解析开关、增量合并导入（zip 增量 upsert + 媒体哈希去重）。
