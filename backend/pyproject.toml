[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.mypy]
python_version = "3.10"
check_untyped_defs = true
warn_unused_ignores = true
no_implicit_optional = true
exclude = [".pylibs"]

[tool.coverage.run]
source = ["travel"]
