# 数据自包含目录，拷贝即迁移

唯一事实来源是一个自包含的数据根目录：`travel.db`（SQLite 单文件）+ `media/` + `thumbs/` 等，目录内只存相对路径、schema 带版本号；换机 = 整体拷贝目录，另提供 zip 导出/导入。否决了“云账号/服务端数据库”和“仅引用照片原路径”两种方案：前者违背本地优先，后者在源文件移动/删除后档案损坏。该决策让“先做 Windows 还是 Android”降级为次要问题，并保证了数据在平台间可移植。

Status: accepted
