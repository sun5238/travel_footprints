# 时间存储：本地挂钟时间 + IANA 时区

旅行记录是“当地的事实”：日志日期是当地挂钟时间，照片 EXIF 是带 UTC offset 的时间戳，出国旅行时两者会冲突。决定所有实体时间以「本地时间 + IANA 时区」存储，并派生 Unix 时间戳用于排序与聚类；日志解析默认使用当前时区，媒体保留 EXIF 时间戳与 offset 原样提取。否决了“全部统一 UTC、展示时按设置转换”（会丢失“当时当地是几点”的语义）和“只存字符串不做时区运算”（排序/聚类会错乱）。该规则影响所有时间相关代码路径，改动需要数据迁移。

Status: accepted
